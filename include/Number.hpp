#ifndef NUMBER_HPP_
#define NUMBER_HPP_

#include <set>
#include <map>

using Number = long long unsigned int;

using NumberSet = std::set<Number>;

using Factorization = std::map<Number, Number>;
// the key is the prime factor
// and the value is its exponent

#endif
